name: Deploy TMP Mail Worker

on:
  workflow_dispatch:
    inputs:
      cf_token:
        description: "Cloudflare API Token"
        required: true
      cf_account_id:
        description: "Cloudflare Account ID"
        required: true
      worker_name:
        description: "Worker Name"
        required: true
        default: "tmp-mail-api"
      db_name:
        description: "D1 Database Name"
        required: true
        default: "tmp_mail"

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      CLOUDFLARE_API_TOKEN: ${{ github.event.inputs.cf_token }}
      CLOUDFLARE_ACCOUNT_ID: ${{ github.event.inputs.cf_account_id }}
      WORKER_NAME: ${{ github.event.inputs.worker_name }}
      DB_NAME: ${{ github.event.inputs.db_name }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup NodeJS
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install deps
        run: npm install

      - name: Read domains.txt
        id: domains
        run: |
          DOMAINS=$(grep -v '^#' domains.txt | grep -v '^$' | sed 's/^[[:space:]]*//;s/[[:space:]]*$//' | paste -sd "," -)
          DEFAULT_DOMAIN=$(grep -v '^#' domains.txt | grep -v '^$' | sed 's/^[[:space:]]*//;s/[[:space:]]*$//' | head -n 1)

          echo "domains=$DOMAINS" >> "$GITHUB_OUTPUT"
          echo "default_domain=$DEFAULT_DOMAIN" >> "$GITHUB_OUTPUT"

      - name: Create/Get D1
        id: d1
        run: |
          set -e

          DB_ID=$(npx wrangler d1 list --json | node -e "
          let data='';
          process.stdin.on('data',c=>data+=c);
          process.stdin.on('end',()=>{
            const list=JSON.parse(data||'[]');
            const db=list.find(x=>x.name===process.env.DB_NAME);
            if(db) console.log(db.uuid||db.id||db.database_id||'');
          });
          ")

          if [ -z "$DB_ID" ]; then
            CREATE_OUTPUT=$(npx wrangler d1 create "$DB_NAME")
            echo "$CREATE_OUTPUT"

            DB_ID=$(echo "$CREATE_OUTPUT" | node -e "
            let data='';
            process.stdin.on('data',c=>data+=c);
            process.stdin.on('end',()=>{
              const match=data.match(/\\\"database_id\\\"\\s*:\\s*\\\"([^\\\"]+)\\\"/);
              if(match) console.log(match[1]);
            });
            ")
          fi

          if [ -z "$DB_ID" ]; then
            echo "Failed to get database_id"
            exit 1
          fi

          echo "database_id=$DB_ID" >> "$GITHUB_OUTPUT"

      - name: Generate wrangler.toml
        run: |
          cat > wrangler.toml <<EOF
          name = "$WORKER_NAME"
          main = "src/index.js"
          compatibility_date = "2026-05-06"

          [vars]
          DOMAINS = "${{ steps.domains.outputs.domains }}"
          DEFAULT_DOMAIN = "${{ steps.domains.outputs.default_domain }}"
          JWT_EXPIRES = "86400"
          MAILBOX_TTL_HOURS = "24"
          MESSAGE_TTL_MINUTES = "60"

          [triggers]
          crons = ["*/5 * * * *"]

          [[d1_databases]]
          binding = "DB"
          database_name = "$DB_NAME"
          database_id = "${{ steps.d1.outputs.database_id }}"
          EOF

          cat wrangler.toml

      - name: Init D1 schema
        run: npx wrangler d1 execute "$DB_NAME" --remote --file=schema.sql

      - name: Deploy Worker
        run: npx wrangler deploy
